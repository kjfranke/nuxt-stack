# Enable devtools layer (design system available on dev/staging)
NUXT_PUBLIC_DEVTOOLS_ENABLED="enabled"

# Language code for HTML lang attribute (default: en)
NUXT_PUBLIC_LANG="en"

# Allowed hosts for Vite dev server (comma-separated)
# Example: VITE_ALLOWED_HOSTS="test.local,dev.example.com"
VITE_ALLOWED_HOSTS=""
